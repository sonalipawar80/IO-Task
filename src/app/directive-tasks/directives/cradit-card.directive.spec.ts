import { CraditCardDirective } from './cradit-card.directive';

describe('CraditCardDirective', () => {
  it('should create an instance', () => {
    const directive = new CraditCardDirective();
    expect(directive).toBeTruthy();
  });
});
