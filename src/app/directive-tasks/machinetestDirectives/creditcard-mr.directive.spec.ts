import { CreditcardMrDirective } from './creditcard-mr.directive';

describe('CreditcardMrDirective', () => {
  it('should create an instance', () => {
    const directive = new CreditcardMrDirective();
    expect(directive).toBeTruthy();
  });
});
