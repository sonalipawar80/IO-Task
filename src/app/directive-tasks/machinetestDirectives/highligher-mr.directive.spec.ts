import { HighligherMrDirective } from './highligher-mr.directive';

describe('HighligherMrDirective', () => {
  it('should create an instance', () => {
    const directive = new HighligherMrDirective();
    expect(directive).toBeTruthy();
  });
});
