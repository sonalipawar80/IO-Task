import { LowercaseMrDirective } from './lowercase-mr.directive';

describe('LowercaseMrDirective', () => {
  it('should create an instance', () => {
    const directive = new LowercaseMrDirective();
    expect(directive).toBeTruthy();
  });
});
