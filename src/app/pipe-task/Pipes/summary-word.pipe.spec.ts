import { SummaryWordPipe } from './summary-word.pipe';

describe('SummaryWordPipe', () => {
  it('create an instance', () => {
    const pipe = new SummaryWordPipe();
    expect(pipe).toBeTruthy();
  });
});
