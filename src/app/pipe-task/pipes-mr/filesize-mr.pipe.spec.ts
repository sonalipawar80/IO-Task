import { FilesizeMrPipe } from './filesize-mr.pipe';

describe('FilesizeMrPipe', () => {
  it('create an instance', () => {
    const pipe = new FilesizeMrPipe();
    expect(pipe).toBeTruthy();
  });
});
