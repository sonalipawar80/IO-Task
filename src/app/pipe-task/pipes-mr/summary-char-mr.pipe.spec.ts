import { SummaryCharMrPipe } from './summary-char-mr.pipe';

describe('SummaryCharMrPipe', () => {
  it('create an instance', () => {
    const pipe = new SummaryCharMrPipe();
    expect(pipe).toBeTruthy();
  });
});
