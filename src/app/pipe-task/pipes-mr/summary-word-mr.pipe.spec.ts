import { SummaryWordMrPipe } from './summary-word-mr.pipe';

describe('SummaryWordMrPipe', () => {
  it('create an instance', () => {
    const pipe = new SummaryWordMrPipe();
    expect(pipe).toBeTruthy();
  });
});
